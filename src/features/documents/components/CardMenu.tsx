import { Button } from '../../../components';

const CardMenu = () => {
  return (
    <div className='bg-blue-300 p-1 rounded-md flex flex-col gap-1'>
      <Button title='info' type='button' theme='primary' onClick={() => {}} />
    </div>
  );
};

export default CardMenu;
